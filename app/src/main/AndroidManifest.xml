<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alex.electrozon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddHomeActivity" />
        <activity android:name=".ComputersActivity" />
        <activity android:name=".AddComputersActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".PurchaseActivity" />
        <activity android:name=".ReceiptActivity" />
        <activity android:name=".TabletsActivity" />
        <activity android:name=".AddTabletsActivity" />
        <activity android:name=".CellPhonesActivity" />
        <activity android:name=".AddCellPhonesActivity" />
        <activity android:name=".WatchesActivity" />
        <activity android:name=".AddWatchesActivity" />
        <activity android:name=".InfoActivity"></activity>
    </application>

</manifest>